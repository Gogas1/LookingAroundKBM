# Nine Sols LookingAroundKBM mod

Allows you to use controller freelook(Illusionary eye, camera offset, camera tilting) ability using keyboard and mouse controls. Press keybind, Ctrl + S by default, configurable using BepInEx configurator(F1 key), and move mouse to control camera